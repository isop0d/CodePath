import { useState } from 'react'
import './App.css'
const API_KEY = import.meta.env.VITE_APP


function App() {
  const [list, setList] = useState(null)
  useEffect(() => {
  
    fetchAllCoinData(){
      fetch
    }

  }, [])

  return (
    <div></div>
  )
}

export default App
